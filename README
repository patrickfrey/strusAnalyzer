strusAnalyzer
=============

Library for document analysis that splits a document into normalized atomic terms that
can be insterted into a strus storage for retrieval.


For using it you have to include "strus/strusAnalyzer.hpp"


And link against at the libraries:

  libstrus_analyzer.so
  libstrus_tokenminer.so
  libstrus_system.so

